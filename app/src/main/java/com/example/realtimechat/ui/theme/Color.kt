package com.example.realtimechat.ui.theme

import androidx.compose.ui.graphics.Color


val blackLight = Color(0xFF000000) // Main text, active icon state
val onSurfaceVariantLight = Color(0xFF7A7A7A) // Secondary text, default icon state
val tertiaryContainerLight =
    Color(0xFFDBD8DB) // Disabled icons, empty states, app version text, self message background
val greyWhisperLight = Color(0xFFECEBEB) // Borders
val secondaryContainerLight = Color(0xFFF2F2F2) // Login text input background, section headings background

val backgroundLight = Color(0xFF58A1F8)
val surfaceLight =
    Color(0xFFEFEFF2) // Button text, top bars background, bottom bars background, items background
val primaryContainerLight = Color(0xFFE9F2FF) // Message link card background

val white = Color(0xFFFFFFFF)



val primaryLight =
    Color(0xFF005FFF) // Primary color, selected icon state, CTA bg, white buttons label, links, instant commands
val accentRedLight =
    Color(0xFFFF3742) // Error text label, notification badge background, disruptive action text and icon
val accentRedLightContainer = Color(0xFFFFDAD6) // Default Material 3 light red error container
val onErrorContainerText = Color(0xFF410002) // Default Material 3 dark text on error container
val accentGreenLight = Color(0xFF20E070) // Online status


// --- Dark Mode Colors ---

val onSurfaceVariantDark = Color(0xFF7A7A7A) // Secondary text, default icon state
val tertiaryContainerDark =
    Color(0xFF2D2F2F) // Disabled icons, empty states, app version text, self message background
val greyWhisperDark = Color(0xFF1C1E22) // Borders
val secondaryContainerDark = Color(0xFF13151B) // Login text input background, section headings background

val backgroundDark = Color(0xFF58A1F8) // App background

val whiteDark =
    Color(0xFF101418) // Button text, Top bars background, Bottom bars background, Items background
val primaryContainerDark = Color(0xFF00193D) // Message link card background


val onPrimaryDark = Color(0xFF005FFF) // Blue in dark mode

val primaryDark =
    Color(0xFF005FFF) // Primary color, selected icon state, CTA bg, white buttons label, links, instant commands
val accentRedDark =
    Color(0xFFFF3742) // Error text label, notification badge background, disruptive action text and icon
val errorContainerDark = Color(0xFF892B2B) // Default Material 3 light red error container
val onErrorContainerDark = Color(0xFFFFDAD6) // Default Material 3 dark text on error container
val secondaryDark = Color(0xFF20E070) // Online status